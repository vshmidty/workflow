{
    "name": "workflow",
    "version": "0.0.1",
    "private": true,
    "scripts": {
        "deploy": "fiori cfDeploy",
        "build:mta": "rimraf resources mta_archives && mbt build --mtar archive",
        "undeploy": "cf undeploy workflow --delete-services --delete-service-keys"
    },
    "devDependencies": {
        "rimraf": "3.0.2",
        "mbt": "^1.2.18"
    }
}
